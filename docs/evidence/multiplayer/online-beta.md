# A4/A5 private online evidence

Decision: **GATED by A3 `GO`; no production online race exists.**

| Field | Evidence |
|---|---|
| Pure room reducer | PASS — native C and service TypeScript replay the same checked-in 43-command valid/invalid fixture to identical result/error and canonical phase, epoch, leader, member and seat state after every step. It spans all 16 commands, supported-ROM compatibility, cancellation, barriers, retry/conflict/CAS precedence, reconnect, custody, leave and close; the broader native and 24,576-command TypeScript campaigns remain green. Native and browser additionally assert one exact clean-release compatibility vector; dirty/malformed provenance and unsupported ROM revisions fail before room I/O. |
| Authenticated carrier foundation | PASS LOCAL / LIVE CHANNEL GATED — native production build admits only mutual direct/one-hop graphs with exact endpoint generations, deterministic routing and bounded opaque forwarding. Mbed TLS and browser WebCrypto share exact canonical transcript/verification-phrase, P-256 pairwise derivation and HKDF/AES-GCM envelope vectors; a direction-bound seal window—not the caller—owns the monotonic sequence across both payload types, browser concurrent seals refuse, and provider failure/counter exhaustion are fail-atomic. Opening requires the complete caller-expected source-to-recipient direction, and every envelope byte, valid-key forged source, stale epoch/generation, recipient and replay has a fail-atomic negative. One-hop admission separately binds the immediate authenticated source generation, so stale-channel fresh-generation claims and malicious routes refuse before fanout. The separate Match signaling socket contains broken-socket delivery, commits replacement generations only after a delivered welcome, and with the dormant same-origin launcher client passes authenticated-target, generation-replacement, hibernation, rate/size and spoof negatives under an exact 15-unit lifetime reservation. Real 2–4 endpoint channels remain post-A3 and production admission stays false. |
| Preflight consensus foundation | PASS LOCAL / LIVE GATED — the production launcher now has a pure bounded gate over one canonical launch-descriptor SHA-256, peer-key transcript, order-independent directed-graph SHA-256, exact epoch/generations, supported-ROM verification, phrase confirmation and admissible channels. Native/browser share one fixed 124-byte authenticated-channel report vector plus an authenticated-direction-bound three-fragment sequence codec over fixed 64-byte payload-type-1 AEAD envelopes, including opaque one-hop forwarding; strict tests cover fragment reorder/duplicate/conflict/stale/padding, cross-source splicing and forged attribution, reordered graph equivalence, reachability equivocation, progress, deterministic typed recovery, disconnect/readiness withdrawal, corrupt state and atomic negatives. It owns no network/UI/engine effect; `READY` cannot bypass the A3 or publisher admission gates. |
| Fake-adapter launcher lobby | PRIOR ABI-v3 PASS / CURRENT ABI-v4 COMPILE + RENDER REFRESH — shared native/browser C model covers 43 cases, 27 actions and all 18 typed recovery states. A native browser-ABI conformance gate previously drove all cases/actions and proved timeout ownership, both phrase decisions, inert preselection, local-only mismatch and fail-atomic live projection normally and under ASan. ABI v4 adds the bounded leader-only expired-invite replacement state; its focused native targets compile and the actual small Emscripten model links, while execution remains queued. The shipped browser renderer consumes a pure presenter fed all 43 authoritative C projections in Node; it proves action order, selection controls, timeout priority, singular/plural count copy, local **Play Here**/**Choose ROM** substitution, immutable output, malformed-model refusal, phrase announcements and assertive mismatch recovery without opening a browser. A successful setup check stops on a bounded three-compound phrase, requires explicit **Words Match**, and offers **Words Differ**, which clears the phrase, stops progression and preserves the room for a fresh secure preflight. Malformed/stale phrase input rejects atomically and rematches repeat comparison. Prior 25-action rendered evidence remains, while the expanded phrase/27-action/invite-expiry desktop gate is pending. |
| Browser MatchRoom control binding | PRIOR LIVE PASS / CURRENT HARDENING RERUN QUEUED — create/link-or-code join, QR/share, rotation, selection, Ready, reconnect and malformed-state recovery passed previously; every live effect is admitted only from the current enabled presentation and all 27 action ids have an explicit route. The current ABI-v4 delta adds an exact immutable live-state/identity schema, U64 and control-tail bounds/final-state correlation, private/unknown-field refusal, monotonic and equal-revision publication checks, same-origin generation-bound invitation custody, clock-skew-safe receipt-relative early expiry, immediate secret/DOM removal, leader-only **New Invitation** recovery, room/service phase correlation, fail-atomic public-state rollback without expired-secret resurrection, stale-revision non-application copy and terminal guest leave after credential revocation. Its exhaustive pure Node gate and browser timer/publication expiry arms are authored and syntax-validated; both focused C model targets compile. Execute them plus the MatchRoom browser journey in the dedicated-desktop batch. Unrendered/stale actions still fail before requests, gallery mutation is evidence-only, production Check Setup cannot simulate a carrier result, and service data cannot expose Start. |
| Release activation boundary | PASS LOCAL — disabled policy is lazy/zero-room-I/O; enabled policy requires same origin, clean provenance and locally SHA-verified US/PAL ROM; dirty/cross-origin inputs fail closed |
| Two-profile first-time journey | PASS AUTOMATED / HUMAN OPEN — isolated profiles use the real local Worker for create/share, role-link join, outage/retry, distinct selections, Ready backtrack/reconvergence, AX and leave; uncoached human timing/cancel/rematch study remains |
| Compatibility/direct graph/reconnect | FOUNDATION PASS LOCAL / LIVE MATRIX GATED — exact native/browser compatibility identity, bounded direct/one-hop routing, generation-bound forwarding replay custody, transcript/SAS and pairwise AEAD vectors pass; real 2–4 endpoint WebRTC, NAT and asymmetric-loss/reconnect evidence waits for A3 `GO` |
| Security/privacy/red-team | LOCAL HANDOFF READY / LIVE + HOSTED EVIDENCE GATED — threat/data maps, strict signaling authentication/generations/bounds, no-payload persistence, cryptographic mutation negatives, zero-cost abuse controls and an exact dated attack-hypothesis handoff are ready for independent review; hosted abuse, provisioned networks and human phrase UX remain |
| Load/chaos/regional/deploy rollback | PARTIAL LOCAL — real Worker passes 64-way exhausted admission, 64-way forged-control rejection with zero reserve use, persisted process restart, weighted HTTP/socket control reserve, lifetime-bounded signaling, static/security-header fallback and literal-zero kill switch. Credentials are room/role-bound before budget/object access. State-only MatchRoom sockets byte-bound hostile UTF-8 and binary frames before close; separate Match signaling is exact-target, sender-injected and capped before upgrade. Broken socket sends/closes are contained after authoritative commits, and replacement signaling generations persist only after welcome delivery before the predecessor closes. Replacement close custody is serialized with generation assignment, overlapping welcome snapshots canonicalize one highest open generation per endpoint, and non-object JSON protocol roots fail as bounded 400s. A 24,576-command seeded reducer campaign requires deterministic mirror execution, valid bounded state, atomic rejection, exact duplicate receipts and reconstruction parity. Complete DO mutation transitions are input-gated; repeated parallel runs prove one-winner Match revision CAS and single-owner Phone Party seat approval. Secret-gated no-store telemetry reports only exact daily aggregates, latches refusal once and has tested 32-day deletion. Thirteen fixed reservation buckets piggyback existing writes, reconstruct admitted units exactly, isolate old-Worker traffic in a legacy bucket and add no flood-amplified metrics write. Two-release Chromium covers waiting-worker skew and atomic offline activation. All 16 Worker/object calls use v1; four legacy readers and unknown-version pre-storage negatives pass. The checked Free-plan edge payload applies its one 30/10-second/IP rule only to `/api/`; static play is excluded and HTML `429` is typed. A strict 64-KiB-bounded provider-v2 reconciliation gate includes Durable Object duration and rejects private fields, internal arithmetic drift, paid billing/overages, nonzero charge, changed Free ceilings and 75% stop lines without reflecting input. A source contract keeps every Durable Object eligible for idle hibernation. The v3 ledger adds fixed aggregate-only synthetic MatchRoom create/join and Phone Party direct/input success-latency thresholds without a server metrics path. The prior clean validation passed 201/201 native CTests and 42/42 service tests; the current signaling, cost and adapter deltas pass their non-GUI gates and the complete service inventory is now 58 tests across 10 files. Hosted canary, edge/version/rollback, real provider exports, distributed raw-request exhaustion, distributed/network chaos and hosted evidence remain. |
| Seven-day weighted quota/currency ledger | V2 CONTRACT + COLLECTOR PASS LOCAL / HOSTED EVIDENCE GATED — exact bounded validator rejects noncontiguous dates, billing/charge, capacity/provider drift, partial legacy tracking, failed local play, synthetic experience under-sampling/success/latency misses, open incidents and non-GO state. The collector restricts origins, creates fresh profiles, drives production HTTP/WebSocket/WebRTC paths and writes only a new fixed aggregate; a one-attempt local smoke succeeds but is correctly non-qualifying. The separate signed human record remains required and fixture/local days never count. |
| Local/Phone Party failure isolation | PASS LOCAL / PHYSICAL EVIDENCE OPEN — disabled release, literal-zero admission and service-blocked routes preserve local/Phone Party actions; connection loss releases or neutralizes remote custody with typed recovery. Physical mixed-source phones and platform/network failure observation remain |
| Reviewer/date/decision | OPEN |
