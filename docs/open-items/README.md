# Open items

Every defect this port has hit, with its mechanism, the measurement that found it,
the fix, and the check that would catch a regression. Sections are in reverse
chronological order by wave — newest first — and **nothing is deleted when it is
fixed**: a closed entry is the only warning the next person gets that the same trap
exists. Retractions are recorded in place rather than removed.

Most of these bugs were **silent**. Read the entry for a subsystem before changing
it.

## Where things live

This was one 422 KB document. It is split by subsystem so that a change to one
area has one place to read and one place to add to. The entries themselves are
unchanged.

| File | Covers |
|---|---|
| [`renderer.md`](renderer.md) | Renderer and visual fidelity — 29 entries |
| [`audio.md`](audio.md) | Audio — 7 entries |
| [`save.md`](save.md) | Saves and progression — 3 entries |
| [`collision.md`](collision.md) | Collision — 3 entries |
| [`allocator.md`](allocator.md) | Allocator and native memory layout — 2 entries |
| [`web.md`](web.md) | Browser (wasm) build — 3 entries |
| [`multiplayer.md`](multiplayer.md) | Multiplayer — 1 entry |
| [`gameplay.md`](gameplay.md) | Gameplay, race and Adventure — 12 entries |
| [`portability.md`](portability.md) | 64-bit (LP64), endianness and portability — 13 entries |
| [`misc.md`](misc.md) | Everything else — 3 entries |


## Contents

### Still open

18 entries below are genuinely open (the former M4.5 WebGPU backend "open
notes" row was merged into the WGPU-11 row below it — every one of its notes
already carried its own FIXED/CLOSED strikethrough, and the only live thread
was the same external-platform validation WGPU-11 already tracks — and four
new entries record campaign completeness, Taj's deferred Time Trial scope,
ghost coverage breadth, and shadow-gate trustworthiness, all previously
disclosed only outside this ledger). The 40 closed ones follow in their own
table.

17 entries below are genuinely open. Two rows in this table are struck through
and kept in place rather than moved: **ghost coverage** closed when
`check_ghost_matrix.py` reached 46 of 47 pairs with the 47th asserted as a
non-producer, and **campaign completeness** largely closed with the
"definitionally done" campaign of 2026-08-07, leaving three named
headless-driving residuals. Both are retained here because a closed entry is
the only warning the next person gets that the trap existed — and because the
`ROADMAP.md` text describing them stayed stale for two days after the gates
landed, which is the specific failure this ledger exists to prevent. One new
entry records unbounded asset sub-entry indices. The 40 closed ones follow in
their own table.

> **Current presentation scope:** the 1.0.1 authored-image containment has been
> superseded by the immutable-presentation wave. Public interpolation owns a
> complete private task and a real forward `{T,T+1}` pair while Original
> gameplay cadence remains fixed. The long fidelity-architecture row below is
> retained as the pre-promotion breadth ledger; its “authored images only”
> production wording is historical. Current design and evidence live in
> [`UNCAPPED_PRESENTATION.md`](../UNCAPPED_PRESENTATION.md). Remaining external
> platform/device breadth is still tracked separately.

| Item | Where |
|---|---|
| **Gameplay cameras can enter terrain and object geometry.** Every ordinary follow mode writes an unconstrained desired eye; late dialogue/shake can move it again; the conditional void detector masks terrain holes rather than resolving the camera and does not include object models. The complete projection-derived lens-guard architecture, fixed-tick authority, terrain/object policy, screen/mode matrix, CAM-00–CAM-09 work plan, and closure gates are specified. **Correction ships opt-in:** `camera_obstruction_runtime_policy()` resolves an unset `MDKR_CAMERA_OBSTRUCTION` to `observe` (a misspelled value resolves there too), and a player opts into the corrected camera through the launcher's `Camera.Obstruction` control, which exports `modern` onto the same variable. It was made the default on 2026-08-07 and reverted the same day when device acceptance found it too sensitive in play, so the defect still stands in default play. It also stays open on breadth: the CAM-00–CAM-09 exit gates cover the pinned routes, not every camera bank and mode, and soft-occluder enrollment is still unbuilt | [camera obstruction modernization plan](../architecture/camera-obstruction.md), [open gameplay record](gameplay.md#open-gameplay-cameras-can-enter-terrain-and-object-geometry) |
| **F-18 independent state reference is PARTIAL; boss cadence policy is CLOSED:** the Ancient Lake lane still has broader open-loop racing-line differences, but its fail-closed real-ROM update/input replay now makes checkpoint clocks 0–3 exact and moves the first five-unit separation from clock 18 to 767, classifying the early mismatch as timestep partitioning. The all-racer Bluey 2 lane isolates the reported boss-speed divergence: retail/Original finish at ticks 3,459/3,458 with a 1.00065× speed ratio; the Enhanced one-field arm finishes at 3,022 with a 1.13982× ratio. Interactive gameplay defaults to Original two-field complete updates, with one-field simulation explicitly available as Enhanced compatibility. Broader challenge, multiplayer, progression/save, audio, renderer-state, and remaining standard-race parity still remain outside this boss lane. **Deliberately deferred:** interactive gameplay defaults to `Gameplay.SimulationCadence=original`, which the independent-emulator Bluey 2 lane measures at 3,458 ticks against the real ROM's 3,459 — a 1.00065× ratio, i.e. retail cadence within one field over a full boss race. The one-field `enhanced` arm that finishes 14% early is opt-in, restart-scoped, and its speed difference is disclosed to players in README before they choose it. What remains open is oracle *breadth* — challenge, multiplayer, progression, audio and renderer-state lanes — not a known divergence in the shipping default. Extending the reference corpus requires an owned ROM and a locally built instrumented emulator per ORACLE.md's hygiene rules, so it cannot run in CI and does not gate a release. A player on defaults races bosses at the speed the cartridge does | [`race_state_oracle.json`](../../tools/oracle_routes/race_state_oracle.json), [`bluey2_state_oracle.json`](../../tools/oracle_routes/bluey2_state_oracle.json), [`BLUEY2_PARITY.md`](../BLUEY2_PARITY.md) |
| **WGPU-11 external/oracle corpus breadth remains open; its local asset, capacity, and fault closeout is DONE** — 46 native menu/course/hub/multiplayer routes execute 249,339,186 strict commands with zero faults, 29 material IDs, 37 material/pipeline keys, and max 3/32 pipelines per material. The offline gate additionally validates all 445 supported-ROM models, 1,146 level segments, and 16,943 batches, including dormant variants; retail high-water is 24/32 vertices and 16/16 triangles. Forced 4 KiB segmentation and a one-entry shader-index limit prove safe continuation, no stale-pointer reuse, one bounded same-backend recovery, and terminal failure without a renderer switch. The 113-point fault registry is fully classified; native runtime gates cover all 83 native-required points and browser gates cover all three browser-required points, while 29 inherited/unemitted paths receive no coverage credit. Browser complete-corpus/minimum-feature hardware, independent state reference, and external native platforms remain. **(Absorbs the former M4.5 WebGPU backend "open notes" row: every note in that section already carries its own FIXED/CLOSED strikethrough, and `gfx_webgpu_set_stage()` has zero production callers — dormant, not open. The only live thread there was this same external-platform validation.) Deliberately deferred:** the local closeout is exhaustive on the platform players actually run: 46 native routes executing 249,339,186 strict commands with zero faults, all 445 supported-ROM models, 1,146 level segments, 16,943 batches, and a 113-point fault registry fully classified with every native-required and browser-required point covered. On macOS that path is Metal — the exact backend a macOS player exercises today. What remains is cross-validation against a sibling project's larger material corpus, a minimum-feature browser-hardware run, an independent state reference, and native execution on Linux and Windows. Every one of those is coverage on hardware and tooling outside the shipping macOS path; none names a suspected defect in it. This is the same class as the F-01/F-10 external-witness debt and closes with hosted infrastructure, not with source changes | [`check_webgpu_content_census.py`](../../tests/check_webgpu_content_census.py), [`check_webgpu_fault_matrix.py`](../../tests/check_webgpu_fault_matrix.py) |
| **M1 residuals remain deliberately deferred:** WebGPU 4x MSAA (IQ-8) and a loader-only, content-free texture-pack path (IQ-11). Supersampling already lowers IQ-8's value; IQ-11 must extend the ROM-absence guard and ship no content. **Deliberately deferred:** IQ-8 (WebGPU 4× MSAA) is deliberately not built because the shipped alternative is already better — `Video.RenderScale` defaults to 2.0 in both Restored and Remastered, and `Video.MSAA` defaults to 0 in every preset because it is redundant with render scale. No aliasing report exists against the WebGPU path. IQ-11 (texture packs) is a feature, not a defect — a loader-only path would ship no content by design, must extend the ROM-absence guard that `tools/check_no_rom.sh` fail-closes on, and building it partially is worse than not building it. `Video.TexturePack` is a documented inert stub, not half-wired code. Neither residual degrades any pixel a player sees today | [`docs/STATUS.md` § imagequality — mipmaps reach the GPU (M1, in progress)](../STATUS.md#imagequality--mipmaps-reach-the-gpu-m1-in-progress) |
| ~~A zip-pad boost reaches **44.9 world units/frame** in an eight-racer Tracks race against **23.2** for the same pad in a solo Time Trial~~ — **CLOSED 2026-07-31, not a defect.** Neither figure reproduces (the route no longer crosses a pad at all). Armed identically, the boost peaks at **22.357** with eight racers and **22.336** solo — 0.09% apart — and all 310 boost/velocity statements in `racer.c` are byte-identical to the decomp baseline. Gated by `tests/check_boost_magnitude.py`. **Still open:** no route exercises a real pad crossing, and no hardware (ares) trace was taken | [wave "zippad"](gameplay.md#closed-not-a-defect-the-zip-pad-boost-is-the-magnitude-dkr-authored--wave-zippad) |
| **The independent Ancient Lake route now drives a real racing line, but strict long-horizon standard-race parity remains open.** Replaying the ROM's observed update widths/input states removes the early cadence mismatch, then sub-unit floating-point drift grows into a different open-loop path; the diagnostic deliberately remains red. The older six pixel routes still cannot show the payoff of the three ROM-fidelity corrections (all moved within ±0.0004) | [wave "closedloop"](gameplay.md#the-fidelity-payoff-measured--and-it-is-not-there), [`ORACLE.md`](../ORACLE.md) |
| **AI stuck-recovery cooldown `unk215` only decays while the kart is moving** (the title long read "while reversing"; `racer->velocity` is negative for forward motion, so the branch means driving forward at speed — corrected by measurement, conclusion unchanged) — matches the decomp (no `GLOBAL_ASM`/`NON_MATCHING`), so believed authored: an AI kart that stops wedged with the cooldown hot can never re-arm its own reverse-out. Measured wedging every corrected fourth-race run at Hot Top Volcano's crater jump. `MDKR_AUTOPILOT_UNSTICK=L7` is a test-only re-arm, now scoped to that level by the guard itself. **Measured on genuine opponents and not reachable**: 32 seeded eight-racer Hot Top Volcano races produced 23 stuck-recovery episodes and 23 recoveries — DKR's own out-of-bounds respawn clears the cooldown, which the autopiloted human never gets because it lands inside the world (`tests/check_ai_unstick_opponents.py`). Hardware/ares verification is the open half | [AI stuck-recovery cooldown](gameplay.md#open-believed-authored-hardware-unverified-ai-stuck-recovery-cooldown-unk215-only-decays-while-reversing--the-hot-top-volcano-crater-wedge) |
| Boss levels 41 and 54 peak at **416** of 500 collision candidates even after the grid-mask fix — 84 slots of headroom. The cap's `j == MAX` test **can no longer be stepped over** (a `j >= cap` pre-check now guards both inserts). **G4 (2026-07-31): now measured and gated, cap deliberately NOT raised** — all ten boss levels plus an ordinary race are swept per-level with zero truncation, and `tests/check_collision_headroom.py` freezes each level's high-water ceiling so headroom cannot quietly shrink. The 84-slot margin itself is unchanged and still worth watching | [wave "boundsweep"](collision.md#swept-three-shapes-no-instrument-could-see--wave-boundsweep), [G4 follow-up](collision.md#g4-follow-up-per-level-headroom-measured-and-gated-cap-not-raised) |
| "Lose animation before the race" / "1st place recorded as a loss" on the first boss. **Symptom 1 (animation before the race) is ruled out by construction:** `level_load()` only ever assigns cutscene 3 or 7, and cutscene 5 (lose) is produced solely by `racer_boss_finish()`'s losing branch. **Symptom 2 (1st place + lose animation) is not reproduced** — `MDKR_BOSS_SLOW=1` measured `finishPosition` and the loaded cutscene tracking correctly in both directions. It is sometimes conflated with wave "bossverdict", but that is a *different, later* report (no cutscene at all, from a save where the win was already recorded); its mechanism explains an absent cutscene, not a lose cutscene, and remains an unconfirmed hypothesis rather than a tested explanation of symptom 2. `tests/check_boss_win_verdict.py` asserts cutscene 4 loads on a genuine win; it asserts nothing about cutscene 5 never appearing on a winning finish, so it would not catch a regression of the originally reported symptom | [wave "gridmask", Report 3 — NOT REPRODUCED](collision.md#report-3--the-lose-animation-played-before-the-race-and-1st-place-recorded-as-a-loss-not-reproduced), [wave "bossverdict"](gameplay.md#not-a-defect-the-boss-win-cutscene-was-skipped-because-the-win-was-already-recorded--wave-bossverdict) |
| Decomp arrays split into two C objects and indexed across the boundary — **class SWEPT**; 3 fixed, most of the rest measured latent, one (`get_inside_segment_count_xz`) recorded unmeasured rather than latent, and `tests/check_array_bounds_sweep.py` now catches new indexed-array cases. **Deliberately deferred:** three sites were fixed and the rest are latent by measurement, not by assumption — peak indices are recorded per site and none reaches its bound on any driven route. The sweep is not a regression list; it fails closed on any new out-of-bounds report at any site not already triaged, across eight routes under UBSan, backed by a static pass over three bug shapes the sanitizer is structurally blind to and by forced-boundary controls that prove the instrument cannot pass vacuously. Rewriting faithful decomp array declarations to satisfy an instrument that already proves they are unreached would trade real fidelity for no measured safety | [wave "splitsweep"](portability.md#swept-decomp-arrays-split-into-two-c-objects-and-indexed-across-the-boundary-wave-splitsweep) |
| **Shadow strength is one preset-wide umbra multiplier, not a per-surface term.** `Video.WorldShadows` (off/soft/full, LIVE, default full) now answers the v0.4 "remaster shadows degrade the UX" report at the level it was made, but the receiver still has no N·L term (away-from-sun faces darken twice), the bias is not slope-scaled, and none of the three umbra values came from a measurement against a reference image. The `MDKR_SHADOW_BIAS`/`MDKR_SHADOW_UMBRA` seams are in place to measure it | [shadow strength residual](renderer.md#open-shadow-strength-is-one-preset-wide-choice-not-a-per-surface-one) |
| **Fidelity architecture: fixed-ticket clock, tick input, catch-up/minimized render elision, independent audio time, nonblocking native GPU production, authority/render boundary, core roots, billboard anchors, compatible model/particle deformation and scalar fades, shield/magnet shear recipes, arbitrary native presentation clocks, and browser rAF parity are closed; presentation breadth remains open.** The host driver owns exact one-/two-field tickets, retains ordinary catch-up debt, rebases suspension time, and prevents late original-mode frames from becoming `updateRate=4/6` mega-steps. Host input uses bounded per-button queues and publishes once per ticket; scripts remain tick-native, browser touch transports between-rAF edges, and overlay/device loss neutralize safely. Audio time accumulates independently in bounded two-field quanta; a presentation cannot manufacture a minimum PCM block, catch-up preserves game-cue order, and suspension rebases retire old audio debt. Its shared queue controller passes deterministic 30–1000 Hz schedules plus wrap/stall boundaries; SDL dummy and a physical five-second CoreAudio/silence run prove exact format, drain, bounded queue, pause, and clear plumbing without exposing copyrighted PCM. Deterministic rational timing plus application late/rebase and arbitrary-rate schedules prove exact drift, alpha, v3 state, ordered gameplay-event, consumed-input, and byte-identical temporary PCM invariance while intermediate catch-up lists are not submitted. Numeric caps 30–1000, `display`, and `uncapped` share one engine grammar; the native application gate proves exact NTSC 30/60/120/144/165/240/uncapped-like counts and PAL 60 without rounding. Production native WebGPU submits only authored tick images, nonblocking-polls completion callbacks, and reports zero runtime completion waits; the browser and internal replay stress retain the two-frame cap, while GL interval 0 keeps its two-fence ceiling. Live arms measure submissions, completions, high-water, runtime versus shutdown waits, achieved rate, holds and failures. Minimized native windows stop real display-list walks and replay, retain input/audio/tick service, and rebase on resume without state/event/input/PCM drift. The browser exposes original/display/numeric ceilings, consumes at most one opportunity per rAF, waits while hidden, and maps a shared native `uncapped` config explicitly to display/FIFO. Real Chromium fixed-144 and irregular schedules preserve state/event/input/PCM, and the complete 3,600-frame lifecycle gate remains green. Render purity compares raw schedules with no state subtraction. Generation-keyed roots, racer heads and vehicle-part child matrices rebuild from immutable snapshots; a bounded retained packet adds billboard-local matrices/world-space anchors for sprite objects/particles, adjacent animated-model vertex batches, direct world-space point/line particle endpoints, and two-lifetime semantic effect recipes. Deformation blends XYZ and shade RGBA only across compatible generations, topology and adjacent ticks; model batches additionally require matching model/animation/root streams. Particle opacity preserves its unsigned 8.8 representation, while sprite/model/line primitive-alpha fades preserve draw-local modifiers and point trails remain single-scaled in retained vertex alpha. Shield/magnet effects additionally require matching racer and shared-effect generations, then rebuild continuous local rotation/scale/shear around the interpolated racer root without matrix-cell lerp. Rewritten billboard keys and ambiguous deformation/effect keys fail closed, while exact multi-viewport particle repeats are idempotent. Independent pixel controls prove root, model-deformation, point-trail geometry/RGBA, primitive-alpha fade and shield-effect output without authoritative drift. Remaining work is audible/loopback DAC qualification across the physical matrix, remaining nested classes and unretained visual scalars, and the rest of the platform/device matrix. `Video.FrameLimit`/`Video.MotionSmoothing` stay restart-scoped while their downstream seams latch. **Deliberately deferred:** motion smoothing ships off and frame limit ships original, so the entire retained-interpolation machinery is inert on defaults and a stock player sees exactly the authored per-tick images. Authored UV scrolling — waterfalls, water and lava — now carries retained {T,T+1} endpoints of its own: the scroll is a per-tick rewrite of the corner UVs inside a Triangle array, so the retained task's copy is one endpoint and the already-authored next task's is the other, and folds are resolved against the whole-texel moduli every driver uses. Batches that cannot be identity-matched hold their authored phase, `waves.c` declares its ping-pong pair rather than leaving the renderer to guess it, and the preview label is retired. Both controls stay restart-scoped for a correctness reason, not an ergonomic one: the presentation policy latches once at first present, and toggling smoothing off mid-run would leave the replay subloop latched on while its arming flag goes false, letting replay copy a stale segment table into live HLE state — a wild-pointer hazard. Making them live is a scoped future slice, not a missing line. The remaining audio work is physical-matrix qualification: application-level queue behavior is proven deterministically across 30–1000 Hz schedules and witnessed on real CoreAudio, but SDL cannot expose the hardware buffer and silence cannot prove speaker output, so audible qualification across every OS and output device stays release-matrix evidence. Nothing here touches saves, ghosts, split-screen or state authority — those are proven byte-identical across every presentation rate and smoothing setting | [`check_fixed_tick_schedules.py`](../../tests/check_fixed_tick_schedules.py), [`check_arbitrary_presentation_rates.py`](../../tests/check_arbitrary_presentation_rates.py), [`check_gpu_backpressure.py`](../../tests/check_gpu_backpressure.py), [`check_surface_suspension.py`](../../tests/check_surface_suspension.py), [`check_browser_presentation_rates.py`](../../tests/check_browser_presentation_rates.py), [`check_browser_runtime.py`](../../tests/check_browser_runtime.py), [`check_render_purity.py`](../../tests/check_render_purity.py), [`check_presentation_matrix.py`](../../tests/check_presentation_matrix.py), [`check_touch_controls.py`](../../tests/check_touch_controls.py) |
| ~~**Campaign completeness (silver coins, later boss rematches, both Wizpig races, and the credits path) is ungated, not unimplemented.**~~ — **largely CLOSED 2026-08-07.** `check_campaign_progression.py` gates every late seam: silver-coin collection by the game's own coin objects and its EEPROM round-trip read back by a second process; all four rematches won on production-persisted saves with `wizpigAmulet` climbing 1-2-3-4; the Wizpig 1 unlock in both directions; and the Wizpig 2 win setting and persisting `bosses & 0x20`. **Three residuals remain**, each a headless-driving obstacle rather than a missing feature: the lobby rematch door driven rather than retargeted (the kart stalls 1,240 units short of the boss exit), `ttAmulet`/trophy chaining stated as fixture premises, and the credits screen reached from a won Wizpig 2 (the cutscene stack holds at `ASSET_LEVEL_WIZPIG2ANIM` for 25,000 frames). Measured obstacles in [`tests/fixtures/README.md`](../../tests/fixtures/README.md#residual-manual-acceptance) | [gameplay record](gameplay.md#open-campaign-completeness--silver-coins-later-boss-rematches-both-wizpig-races-and-the-credits-path-are-ungated-not-unimplemented) |
| **Asset sub-entry indices are unbounded, and the one bounds check that exists fails silently.** Section indices are checked; indices *within* a section are not. `tools/sweep_subentry_access.py` measures 276 sites: 129 checked, 65 unchecked, 82 unprovable. Three concrete findings — `particles.c:739,757,758` (guards live in both callers, not in `emitter_init_with_pos` itself), `objects.c:1474,4382` (no comparison at all), and the 75 `get_misc_asset()` sites that read as checked while the accessor returns the **section base silently** out of range, converting an out-of-range read into a confidently wrong one. **Measured not reachable today:** unsupported revisions are refused by name before engine boot and no override exists, so on a supported ROM every index is in range by construction. The ordering is a safety property — the check must land before the supported ROM set widens | [portability record](portability.md#open-asset-sub-entry-indices-are-unbounded-and-the-one-bounds-check-that-exists-fails-silently--wave-subentry) |
| **Taj Time Trial records no best time and stores no ghost, silently.** Deliberate containment, per [`docs/architecture/taj-playable-mod.md`](../architecture/taj-playable-mod.md) §352-369/§535-545: Taj is a virtual character overlaid on the retail roster, not a true `CHARACTER_TAJ` slot, so competitive timing data is quarantined to protect existing saves/ghosts/records rather than by oversight. **Deliberately deferred:** giving Taj real ghost/leaderboard semantics is a save-compatibility question — how a non-retail character is represented in a format retail hardware also reads — not a bug fix, and belongs with the authored vehicle rows and fourth carpet physics class in a second-generation asset-pack feature | [gameplay record](gameplay.md#open-deliberately-deferred-taj-time-trial-records-no-best-time-and-stores-no-ghost) |
| ~~**Ghost read/write coverage is one (track, vehicle) pair of 47.**~~ — **CLOSED.** `check_ghost_matrix.py` drives 46 of the 47 legal pairs through a record, a save, and a read-back in a **fresh process**, each in its own save directory. The 47th, Spaceport Alpha in the car, is an asserted autopilot non-producer rather than a skip: its racing line dead-ends at `courseCheckpoint` 10 and DKR records a ghost only under 10,800 frames, so the check fails if it ever starts finishing. The serialized ghost layout is untouched. Retained here because the underlying trap is worth the warning — this exact function once shipped a stack-buffer-overflow that aborted with nothing on stderr | [gameplay record](gameplay.md#open-ghost-coverage-is-one-track-vehicle-pair-of-47) |
| **Shadow gate trustworthiness: the harness environment has twice diverged from the shipping build and let a real defect through green gates.** Wave "shadowplay"'s four root causes were found only by manual playthrough while `check_world_shadows.py` and its siblings passed throughout; wave "shadowdeep" R1 found that every registered shadow gate set `MDKR_TRACE=1`, which is why none of them saw a shipping-build-only static-caster-cache leak. Both underlying bugs are fixed. **Deliberately deferred:** what remains is a gate-infrastructure change — auditing every gate that exports a diagnostic env var the shipping build does not set, and adding a shipping-configuration arm to the shadow gates — not a rendering fix, and it touches the whole renderer suite rather than one file | [renderer record](renderer.md#open-shadow-gate-trustworthiness--harness-environment-has-diverged-from-the-shipping-build-twice) |
| **446 vendored decomp symbols are still spelled as ROM addresses.** 154 `func_` and 292 `D_` across 2550 sites in `game/`; 84 of them now carry evidence-backed readable aliases in `game/include/decomp_names.h`, covering 63 of the 64 the port actually exercises. The rest is archaeology, and the rule is **never blind-rename**: a name without a citation is worse than the address, and the audit that opened this wave got two of its own examples backwards. Renaming in vendored text is also permanent merge-conflict surface, so names are aliases, never in-place edits | [misc record](misc.md#not-a-defect-but-a-standing-readability-hazard-446-vendored-symbols-are-still-spelled-as-rom-addresses--wave-decompnames) |
| **Save states are blocked on payload scope, not on pointers.** A raw arena snapshot restores correctly at a different host base address across five ASLR bases, so the 32-bit-token argument holds; but the sufficient payload is the arena plus every externally-linked writable global defined by a `game/src` TU — 1,686 symbols, 808,964 bytes — and neither an enumerated list nor a link-order-derived region is an invariant this project has paid for. The container ships and is unit-tested; nothing captures or restores, and `F5`/`F7` are unbound | [open save-state record](save.md#open-save-state-capture-is-blocked-by-payload-scope-not-by-pointer-tokens--wave-savestate) |


### Closed, kept as a record

Struck-through text is the original problem statement; the text after it is
what was done and what proves it.

| Item | Where |
|---|---|
| ~~**Opening the app overlay could abort an attract race or drop a race-intro camera.**~~ — **FIXED:** zero-rate racer physics is rejected at the racer boundary while menu camera objects still republish; the app pause retains the last authored cutscene-camera bank; the former render-owned three-player TT clear is conditioned on its exact retail predicate. Live failing controls reproduce the Greenwood Village NaN abort and a 73.166-level flyover-to-kart camera snap; both fixed arms freeze exactly and resume | [issues #28/#29, wave "overlaypause"](gameplay.md#fixed-the-app-overlay-crashed-attract-races-and-dropped-race-intro-cameras--issues-2829-wave-overlaypause) |
| ~~Deferred integration work: wire the `asset_swap_normalize` / `asset_swap_lut` hooks into the loader~~ — **DONE.** M2 wired the LUT and `asset_table_load` hooks; M3 wired the per-caller post-inflate and whole-record hooks. `asset_swap_normalize()` is called from `asset_loading.c`, `game.c`, `object_models.c`, `objects.c`, `textures_sprites.c`, `tracks.c` and `racer.c`, and gzip'd sections swap after inflate | [Integration pending](misc.md#integration-pending) |
| ~~**Every magic code was rejected and the enabled list showed `8` plus a blank row**~~ — **FIXED/HARDENED:** reproduced as the decrypted table's big-endian u16 count/offsets being consumed natively. Clean `v1.0.0` source already carries the original normalization repair, proving the reported DMG was not a reproducible tag artifact; complete offset/string validation, rollback, a ROM-free exact-symptom unit, and commit/version/checksum-bound release provenance now fail closed | [magic-code endian/artifact investigation](portability.md#fixed-tagged-macos-artifact-exposed-a-stale-magic-code-endian-failure) |
| ~~**The macOS DMG produced Finder's “damaged” rejection instead of the expected unidentified-developer warning**~~ — **FIXED in the packaging path:** post-link mutation is followed by inside-out ad-hoc sealing; every intentionally unsigned patch app must pass strict nested/resource/load-path checks plus a LaunchServices/WebGPU present-and-capture smoke from the read-only mounted DMG. An optional protected Developer ID/notarization lane remains publication-disabled until it has an equivalent post-sign runtime gate; it is not required for this ad-hoc patch | [macOS damaged bundle](portability.md#fixed-macos-packaged-app-was-reported-as-damaged) |
| ~~**Remastered had no real world-shadow feed, WebGPU receiver path, filmic finish, or per-world grade**~~ — **DONE on GL/WebGPU/browser:** one immutable capture from the real traversal feeds stable 1P–4P cascades; receiver readiness hands off atomically from projected decals; playable worlds receive a bounded linear-light finish while authored 2D stays outside. The inherited contact-AO path is explicitly **CUT**, not silently open: its projection is never published, a forced-on control changes zero pixels, and the backend algorithms do not share the required reconstruction/filter contract | [`COLOUR_LIGHTING_PIPELINE.md`](../COLOUR_LIGHTING_PIPELINE.md) |
| ~~**The advertised Linux GCC cells rejected Clang-only warning names, while a real alternate-compiler build exposed pointer/scalar type confusion and indeterminate audio, collision, camera, AI, plane, formatting, and sequence state**~~ — **DONE locally:** compiler-family flags are split correctly; both Linux workflow cells explicitly select GCC; GCC 16 with final `-O1` and explicit full UBSan passes 24 CTests, all 47 legal vehicle routes, challenges, 3P/4P, filename entry, and a 7,500-frame soak. Debug, Release, ASan, wasm, Chromium, and the 46-route WebGPU census pass afterward. The remaining F-01/F-10 qualification is the first external hosted witness and repository branch protection, not a known source failure | [`check_full_ubsan.py`](../../tests/check_full_ubsan.py), [`correctness.yml`](../../.github/workflows/correctness.yml) |
| ~~**Stage teardown retained stale pointer mappings and invalidated texture-cache entries leaked their backend objects**~~ — **DONE on available native GL/WebGPU/browser:** teardown retains already-queued global pointer tokens for one generation, expires unobserved mappings at the next boundary, and rebuilds without tombstones; cache invalidation and failed uploads delete owned GPU textures and clear bound state. This corrects the 3P/4P `dRspInit` token/segment collision that the earlier report misclassified as an intentional unterminated child. Native and real-wasm four-race gates prove stable main/audio heaps, exact physical/music/jingle/SFX conservation, coherent non-growing renderer generations, and stable registry high-water. Cooperative shutdown releases frontend/backend children, host roots, ROM/arena/delayed-free/renderer scratch, SDL, and the AudioWorklet in dependency order. Typed ROM/arena/opaque-message domains and a raw-narrowing source gate complete F-12; lifecycle ownership completes F-19 | [`check_resource_plateau.py`](../../tests/check_resource_plateau.py), [`check_browser_resource_plateau.py`](../../tests/check_browser_resource_plateau.py) |
| ~~**Controller presence, original ghost storage, and rumble were stubbed**~~ — **IMPLEMENTED on available hosts:** truthful P1–P4 status, SDL/browser haptics with disconnect cancellation, four atomic checksummed virtual Paks, bounded ghost parsing, real post-race save/fresh-process load, and browser full-set backup/import with controlled rollback. Physical multi-pad/haptic platform breadth remains matrix evidence | [Virtual Controller Pak and ghost custody](../VIRTUAL_CONTROLLER_PAK.md) |
| ~~**Browser save ownership was incomplete, and a refresh could persist a checksum-invalid intermediate Adventure slot**~~ — **DONE in the foundation/save-custody tranche:** ROM/renderer-independent raw and portable export, strict import, semantic editor, corrupt-block recovery/merge, native CLI parity, exact rollback verification, three automatic checksum-safe generations, block-granular boot recovery, and awaited IDBFS durability. Full site-data loss still requires the deliberately user-controlled downloaded backup | [player workflow](../SAVE_MANAGEMENT.md) |
| ~~**RL-2 must derive a level-owned sun without exposing coarse triangle facets**~~ — **DONE**: the stable rig comes from normalized `LevelHeader`/`LevelHeader_70` data with no per-level table; racer/character fragments use the models' compact smooth-normal streams and retain baked colour as ambient. GL/WebGPU A/B covers two worlds, Pure/Restored stay exact, and Chromium asserts the production path | [Wave 2 colour and lighting contract](../COLOUR_LIGHTING_PIPELINE.md) |
| ~~**WebGPU surface/device lifecycle was not recoverable after real bring-up**~~ — **DONE on available platforms:** explicit surface-status and usage policy, fallible frame unwinding, one native WebGPU device rebuild followed by a clean terminal stop if recovery fails again, stable browser error UI, featureless far clipping, typed native handles, and explicit artifact tuples. GL remains available only by explicit diagnostic selection. Linux/Windows execution remains matrix debt | [`architecture/webgpu.md`](../architecture/webgpu.md) |
| ~~**UI-3 resolution-independent HUD and UI-4 in-game video options**~~ — **DONE on GL/WebGPU/browser:** the final authored 2D layer renders after world resolve at physical output resolution; safe 4:3, full-bleed, legacy stretch, FOV, 1P–4P, memory-blend, readback, resize, and fault lifecycles are preserved. The localized in-game screen now exposes presentation, supersampling, aspect/FOV, filtering, Remaster effects, and subtitles with atomic persistence, truthful live/restart behavior, startup-override locks, and fail-closed storage faults. A same-binary control measures 27–30% more HUD edge detail with exact sampled world pixels | [`check_native_ui_resolution.py`](../../tests/check_native_ui_resolution.py), [`check_video_options.py`](../../tests/check_video_options.py) |
| ~~**Texture/CI construction, custom-FX ownership, level token arithmetic, libultra numeric UB, the audited data-derived shifts, and save-derived model selectors were unsafe at their final use sites**~~ — **DONE**: transactional capacities and rollback, dry audio fallback, typed token/address boundaries, exact MIPS conversion semantics, range-checked logical bitsets, and model-count clamps; ROM-free boundaries plus Debug/Release/ASan/limited-UBSan/audio/wasm gates | [wave "core-safety"](portability.md#fixed-core-safety-boundaries--wave-core-safety) |
| ~~**Variable-frame sprite allocation could under-reserve native storage**~~ — **DONE**: one checked serialized-record layout owns the builder and validates every sprite-asset reader; ROM census found the old formula short by two `Gfx` commands (16 bytes) on sprite 162 and one command (8 bytes) on sprite 177 | [restoration/remaster sprint](renderer.md#fixed-restorationremaster-sprint--sprite-bounds-rdp-gradients-sdf-text-moving-mips-and-rl-1) |
| ~~**RDP shade/fog gradients were perspective-interpolated and clipped fog was interpolated after derivation**~~ — **DONE**: shader options select screen-linear interpolation and clipped vertices recompute fog from clip space; exact legacy control retained | [restoration/remaster sprint](renderer.md#fixed-restorationremaster-sprint--sprite-bounds-rdp-gradients-sdf-text-moving-mips-and-rl-1) |
| ~~**Level teardown, racer fallback state, sound groups/IDs, and special-vehicle engine rows crossed unchecked runtime boundaries**~~ — **DONE**: owner-last teardown; defined trick/item/path fallbacks; exclusive audio bounds; explicit flying-car/loop mappings; ROM-free exhaustive domains plus a mutation-controlled production census | [wave "runtimebounds"](portability.md#fixed-playable-runtime-boundaries--wave-runtimebounds) |
| ~~**The central allocator formed sentinel pointers, wrote past the delayed-free queue, accepted invalid sizes, and narrowed host addresses**~~ — **DONE**: checked size/pool/range invariants, transactional fixed splits, grow-before-write delayed frees, fail-fast safe allocation, and a Debug/Release/ASan/UBSan property gate | [wave "allocinv"](allocator.md#fixed-central-allocator-boundaries--wave-allocinv) |
| ~~**Native object tails, serialized level records, and renderer-prefix casts relied on N64 alignment/adjacency**~~ — **DONE**: one checked native layout cursor, validated variable-record boundary, field-oriented render APIs, exact UBSan/ASan legacy controls, and a full alignment gameplay matrix. The same wave found and fixed an eight-byte map boost record reading byte eight from the next record | [wave "nativelayout"](allocator.md#fixed-native-representation-boundaries-object-tails-level-records-and-render-proxies--wave-nativelayout) |
| ~~**`object_functions.c:3463` shifted by an id that can be `-1`**~~ — **DONE**: the shared unsigned course-flag helper accepts exactly IDs 0..15; a balloon whose allocator did not produce a valid ID is removed before the flag access, and door/trigger use the same contract | [wave "core-safety"](portability.md#fixed-core-safety-boundaries--wave-core-safety) |
| ~~**`courseFlagsPtr` low half was decoded as a 3-step ladder and re-encoded as a 3-term count**~~ — **DONE:** the engine and portable codec share an ordered status contract. Encoding retains only the longest valid prerequisite prefix, so all four legal states round-trip exactly and all eight low-bit combinations are exhaustively proven unable to gain progress | [save progression/persistence isolation](save.md#fixed-save-progression-and-persistence-isolation) |
| ~~**Two trophy lookups had negative shift counts at `worldId == 0`**~~ — **DONE**: both use a shared world-1..4 extractor; invalid worlds produce no trophy selection and execute no shift | [wave "core-safety"](portability.md#fixed-core-safety-boundaries--wave-core-safety) |
| ~~**`SaveConfig.subtitles` named the wrong bit**~~ — **DONE:** bit 24 is explicitly `defaultHigh`, bit 25 is `subtitles`, the remaining unknown field is 30 bits, and compile-time assertions lock the original 40-byte slot/8-byte config layout | [save progression/persistence isolation](save.md#fixed-save-progression-and-persistence-isolation) |
| ~~**`trackmenu_set_records()` could contaminate persisted progress through later force-save ordering**~~ — **DONE:** normal writes are Adventure-only; every forced cart copy now carries an explicit `gSavefileData[]` source and every Pak import uses the dedicated fourth scratch slot. The aggregate live `Settings` object is no longer an eligible forced-write source | [save progression/persistence isolation](save.md#fixed-save-progression-and-persistence-isolation) |
| ~~**`func_80017A18` is a `return 0` stub, so object-model collision never reports a hit**~~ — **DONE**, and it closes the "closed door can be driven past" item with it. Upstream matched the body (`9da89ecb`); the port needed two `DKR_PTR` lines. Every collision-meshed object was intangible and `collidedObj` was permanently NULL, killing five interaction sites | [wave "objcoll"](collision.md#fixed-object-model-collision-never-reported-a-hit-so-locked-doors-were-intangible--wave-objcoll) |
| ~~On the **three-lap Adventure** Ancient Lake race the human reaches lap 3 but never finishes~~ — **RETRACTED / COVERAGE FIXED**: the probe used starting-grid slot zero and followed an AI. Natural finishing place is no longer treated as a stable oracle; symmetric post-finish verdict arms verify identity, return, checksum-valid course status, and exact balloon counts | [wave "adventurefinish"](gameplay.md#retracted-the-adventure-three-lap-race-finishes-normally--wave-adventurefinish) |
| ~~`mtxf_to_mtx` and `arctan2_f` truncated where the ROM's `cvt.w.s` rounds to nearest, while `atan2_lookup` rounded a ratio the ROM truncates~~ — **DONE together:** explicit MIPS nearest-even conversion with indefinite invalid result; exact integer lookup index; exhaustive ROM-free ratio/tie tests; assembly-derived table gate and a 7,500-frame live race pass | [MIPS numeric conversion closeout](portability.md#fixed-mips-numeric-conversion-closeout) |
| ~~An Adventure-path **first** boss win had never been driven end to end~~ — **DONE**: a checksum-valid three-race checkpoint drives the fourth Dino race, the four-balloon boss door, a physical Tricky 1 finish, win/loss cutscenes, hub return, EEPROM, and fresh-process reload. The old checkpoint-37 stock-AI miss is retained as the required failing control | [`check_first_boss_progression.py`](../../tests/check_first_boss_progression.py) |
| ~~`collision_objectmodel` `spB4[10]`/`sp8C[10]` mismatched a count capped at 20~~ — **DONE:** both arrays now hold the full 20-object candidate set; the measured peak remains 1, but dense content can no longer overflow the stack | [wave "splitsweep"](portability.md#swept-decomp-arrays-split-into-two-c-objects-and-indexed-across-the-boundary-wave-splitsweep) |
| ~~`get_inside_segment_count_xz()` writes through an unbounded pointer~~ — **DONE**, and the class with it: 27 bare-pointer writers enumerated, 5 bounded — including `collision_get_y`, which the report did not mention and which has **five callers with five different capacities** | [wave "boundsweep"](collision.md#swept-three-shapes-no-instrument-could-see--wave-boundsweep) |
| ~~The bare-pointer class was bounded but two members sat at **4 slots of measured slack**~~ — **RESOLVED:** all four ordinary `collision_get_y` callers now use 16 slots and the owning gate measures at least 11 slots of live slack; its fifth wave-builder caller retains 30. `segmentsInside[8]` intentionally retains the ROM's explicit `>= 8` rejection policy, with writes bounded before that decision | [wave "boundsweep"](collision.md#swept-three-shapes-no-instrument-could-see--wave-boundsweep) |
| ~~Required gameplay/math providers could resolve to weak no-op fallbacks, and invalid animation targets were silently skipped~~ — **DONE:** no-op providers are removed, validated math/RNG providers are strong, linked/source symbol checks reject fail-open providers, and animation setup enforces behavior type, header/runtime identity, and complete allocation bounds. All 20 tracks initialize targets with zero invalid bindings; menu, challenge, and boss routes also pass | [`check_math_tables.py`](../../tests/check_math_tables.py), [`check_track_sweep.py`](../../tests/check_track_sweep.py) |
| ~~`save_data.c`'s `func_800756D4` had four unbounded output pointers~~ — **DONE:** the API now carries an explicit output capacity, validates all four arrays, and the sole menu caller passes its six-element extent. `savemenu_blank_save_destination` remains with its owning menu/save wave | [Virtual Controller Pak and ghost custody](../VIRTUAL_CONTROLLER_PAK.md), [wave "boundsweep"](collision.md#swept-three-shapes-no-instrument-could-see--wave-boundsweep) |
| ~~Adventure trophy round/rankings/award flow was not reachable~~ — **DONE**: all four worlds, all 16 rounds, stable ties, gold/silver/bronze/no-award, quit/retry, cinematic, EEPROM write and fresh-process reload are production-driven; this also found and fixed the rankings QUIT option being ignored | [P3.5 Adventure](gameplay.md#p35-adventure--the-hub-is-drivable-and-the-trophy-series-is-covered) |
| ~~Wave 3's original gameplay envelope was incomplete~~ — **DONE, 6/6**: 3P/4P, Adventure Two, all authored challenge/battle courses, the legal first boss, all Taj vehicle challenges, and all four trophy series have registered both-direction gates | [`DEVELOPER_HANDBOOK.md` completion snapshot](../DEVELOPER_HANDBOOK.md#waves-13-completion-snapshot) |
| ~~`objects.c` object spawner used save-derived values as model indices with no clamp against `numberOfModelIds`~~ — **DONE**: the spawner's character/trophy/amulet/signpost selectors share one final-use model/count resolver, and T.T. doors independently bound both the amulet table and object-header model list | [wave "core-safety"](portability.md#fixed-core-safety-boundaries--wave-core-safety) |
| ~~`[EVTQ] post DROPPED` in the browser~~ — **DONE**: a real Chromium/AudioWorklet gate measures all three queues; SFX demand reached 195, its port budget is 512, and the gate requires zero drops plus >2× live headroom | [EVTQ post DROPPED](audio.md#fixed-browser-audio-event-queue-drops--measured-in-the-real-runtime) |
| ~~3 of 5 DKR revisions still refused~~ — **ACCEPTED PRODUCT SCOPE:** US 1.1 and European 1.1 are the frozen supported set. US 1.0, European 1.0, and Japanese are recognized and named but safely refused; adding them requires the explicit expansion gates rather than a boolean flip | [ROM_REVISIONS.md §6](../ROM_REVISIONS.md) |
| ~~`__ROM_END` was us.v80's and not re-pointed per revision~~ — **DONE:** every revision descriptor owns its linker-generated ROM extent; the loader validates and selects it alongside the LUT, and the native cheat-menu checksum consumes the selected value rather than a host address or US constant | [`../platform/rom_id.c`](../../platform/rom_id.c) |
| ~~**v0.4 playthrough: phantom shadows from invisible geometry, hash-invented sun heading, floating pickups, heavy umbra**~~ — **FIXED (wave "shadowplay", 2026-07-29)**; the recorded aliasing residual was then confirmed live and closed by wave "shadowdeep" | [wave "shadowplay"](renderer.md#fixed-v04-playthrough-shadow-defects--wave-shadowplay) |
| ~~**Pre-release deep review: shipping builds never reset the shadow static cache, the void curtain froze into it as a phantom wall, wave lava/water cast opaque shadows, bias scaled with the stage z-span, hard far terminator, WebGPU clip/GL pancake asymmetry, one draw per cached triangle**~~ — **FIXED (wave "shadowdeep", 2026-07-29)** | [wave "shadowdeep"](renderer.md#fixed-pre-release-shadow-deep-review--wave-shadowdeep) |
| ~~**Mobile touch controls have no registered automated gate.**~~ — **DONE (2026-07-29):** `tests/check_touch_controls.py` is registered as a browser-role task with exactly the specified shape: a desktop gating control, a persisted-"shown" revival arm (which the pre-`dc5f83b` shell demonstrably fails), and a CDP `Input.dispatchTouchEvent` three-finger chord that must reach `osContGetReadData P1` with `btn` carrying A+R (measured `0x8110` with the D-right mirror) and `sx>=60`, then return to exact neutral, with zero page failure markers. Physical-device breadth remains external matrix evidence | [`check_touch_controls.py`](../../tests/check_touch_controls.py) |

### By subsystem, across all files

**Gameplay, race and adventure**

- [OPEN: gameplay cameras can enter terrain and object geometry](gameplay.md#open-gameplay-cameras-can-enter-terrain-and-object-geometry)
- [FIXED: object-model collision never reported a hit, so locked doors were intangible — wave "objcoll"](collision.md#fixed-object-model-collision-never-reported-a-hit-so-locked-doors-were-intangible--wave-objcoll)
- [FIXED: a one-shot cutscene replayed for ever, because its latch was undefined behaviour the optimiser deleted — wave "keyshift"](gameplay.md#fixed-a-one-shot-cutscene-replayed-for-ever-because-its-latch-was-undefined-behaviour-the-optimiser-deleted--wave-keyshift)
- [FIXED: three ROM-fidelity divergences, and the fixture class that was blocking them — wave "closedloop"](gameplay.md#fixed-three-rom-fidelity-divergences-and-the-fixture-class-that-was-blocking-them--wave-closedloop)
- [NOT A DEFECT: the boss win cutscene was skipped because the win was already recorded — wave "bossverdict"](gameplay.md#not-a-defect-the-boss-win-cutscene-was-skipped-because-the-win-was-already-recorded--wave-bossverdict)
- [OPEN, believed authored (hardware-unverified): AI stuck-recovery cooldown `unk215` only decays while reversing — the Hot Top Volcano crater wedge](gameplay.md#open-believed-authored-hardware-unverified-ai-stuck-recovery-cooldown-unk215-only-decays-while-reversing--the-hot-top-volcano-crater-wedge)
- [CLOSED, NOT A DEFECT: the zip-pad boost is the magnitude DKR authored — wave "zippad"](gameplay.md#closed-not-a-defect-the-zip-pad-boost-is-the-magnitude-dkr-authored--wave-zippad)
- [SWEPT: three shapes no instrument could see — wave "boundsweep"](collision.md#swept-three-shapes-no-instrument-could-see--wave-boundsweep)
- [Hand-asm transcription audit — wave "hasmaudit"](portability.md#hand-asm-transcription-audit--wave-hasmaudit)
- [FIXED: racers fall through Tricky's volcano — the collision grid mask never filtered in Z (wave "gridmask")](collision.md#fixed-racers-fall-through-trickys-volcano--the-collision-grid-mask-never-filtered-in-z-wave-gridmask)
- [OPEN: save-state capture is blocked by payload scope, not by pointer tokens — wave "savestate"](save.md#open-save-state-capture-is-blocked-by-payload-scope-not-by-pointer-tokens--wave-savestate)
- [Save-file fail-safe and browser save recovery — wave "savefailsafe"](save.md#save-file-fail-safe-and-browser-save-recovery--wave-savefailsafe)
- [P3.6 two-player split-screen — wave "splitscreen" (WORKS)](multiplayer.md#p36-two-player-split-screen--wave-splitscreen-works)
- [P3.5 Adventure — the hub is drivable and the trophy series is covered](gameplay.md#p35-adventure--the-hub-is-drivable-and-the-trophy-series-is-covered)
- [OPEN: campaign completeness — silver coins, later boss rematches, both Wizpig races, and the credits path are ungated, not unimplemented](gameplay.md#open-campaign-completeness--silver-coins-later-boss-rematches-both-wizpig-races-and-the-credits-path-are-ungated-not-unimplemented)
- [OPEN, deliberately deferred: Taj Time Trial records no best time and stores no ghost](gameplay.md#open-deliberately-deferred-taj-time-trial-records-no-best-time-and-stores-no-ghost)
- [Playability wave — memory safety, saves, and the race finish](gameplay.md#playability-wave--memory-safety-saves-and-the-race-finish)
- [OPEN: ghost coverage is one (track, vehicle) pair of 47](gameplay.md#open-ghost-coverage-is-one-track-vehicle-pair-of-47)
- [Race gameplay — tiny racer models + crawl speed — FIXED (single root cause)](gameplay.md#race-gameplay--tiny-racer-models--crawl-speed--fixed-single-root-cause)
- [High-rate native delivery cadence — CLOSED (immutable-presentation wave)](renderer.md#high-rate-native-delivery-cadence--closed-immutable-presentation-wave)
- [Frame pacing / slow-motion — RESOLVED (pacing wave)](renderer.md#frame-pacing--slow-motion--resolved-pacing-wave)

**Renderer and visual fidelity**

- [FIXED: three effects drew zero pixels, because hand-packed triangles were built in N64 byte order](renderer.md#fixed-three-effects-drew-zero-pixels-because-hand-packed-triangles-were-built-in-n64-byte-order)
- [FIXED: PAL was mapped as if it were NTSC — a fixed 240-line surface and no clip-ratio overdraw](renderer.md#fixed-pal-was-mapped-as-if-it-were-ntsc--a-fixed-240-line-surface-and-no-clip-ratio-overdraw)
- [FIXED: `G_VTX_APPEND` is a base, not a running cursor — the floating intro shrubs](renderer.md#fixed-gvtxappend-is-a-base-not-a-running-cursor--the-floating-intro-shrubs)
- [FIXED: the cascade light-depth axis pointed at the sun — wave "shadowsign"](renderer.md#fixed-the-cascade-light-depth-axis-pointed-at-the-sun--wave-shadowsign)
- [OPEN: shadow strength is one preset-wide choice, not a per-surface one](renderer.md#open-shadow-strength-is-one-preset-wide-choice-not-a-per-surface-one)
- [OPEN: shadow gate trustworthiness — harness environment has diverged from the shipping build twice](renderer.md#open-shadow-gate-trustworthiness--harness-environment-has-diverged-from-the-shipping-build-twice)
- [FIXED: restoration/remaster sprint — sprite bounds, RDP gradients, SDF text, moving mips, and RL-1](renderer.md#fixed-restorationremaster-sprint--sprite-bounds-rdp-gradients-sdf-text-moving-mips-and-rl-1)
- [FIXED: widescreen world billboards retained the N64's 4:3 stretch — wave "billboardaspect"](renderer.md#fixed-widescreen-world-billboards-retained-the-n64s-43-stretch--wave-billboardaspect)
- [FIXED: "interlaced" textures decoded scrambled — the odd-row TMEM word swap (wave "lineswap")](renderer.md#fixed-interlaced-textures-decoded-scrambled--the-odd-row-tmem-word-swap-wave-lineswap)
- [M4.5 WebGPU backend — DONE (qualified fail-closed native default; browser renderer). Open notes:](renderer.md#m45-webgpu-backend--done-qualified-fail-closed-default-open-notes)
- [Phase 2 — menu 1:1 fidelity: every screen now scored (wave "oraclefix")](renderer.md#phase-2--menu-11-fidelity-every-screen-now-scored-wave-oraclefix)
- [Menu 1:1 fidelity (frontend Timber's Island background) — wave "menufi"](renderer.md#menu-11-fidelity-frontend-timbers-island-background--wave-menufi)
- [P3.3 MAGIC_CODES / FILE_SELECT text fidelity — wave "p33-text"](renderer.md#p33-magiccodes--fileselect-text-fidelity--wave-p33-text)
- [FIXED: no near-plane clipping in the HLE (wave "nearclip")](renderer.md#fixed-no-near-plane-clipping-in-the-hle-wave-nearclip)
- [F3DDKR semantics pinned down (M3c renderer wave — text/logo/sprites)](renderer.md#f3ddkr-semantics-pinned-down-m3c-renderer-wave--textlogosprites)
- [M4 render state (this wave — input + interactive menus)](renderer.md#m4-render-state-this-wave--input--interactive-menus)
- [M3 rendering state (UPDATED — M3b renderer bring-up)](renderer.md#m3-rendering-state-updated--m3b-renderer-bring-up)
- [From F3DDKR renderer workstream (M3-gfx, commit a8dcd00)](renderer.md#from-f3ddkr-renderer-workstream-m3-gfx-commit-a8dcd00)

**Audio**

- [FIXED: an AudioWorklet underrun left the repair envelope out of the loop](audio.md#fixed-an-audioworklet-underrun-left-the-repair-envelope-out-of-the-loop)
- [FIXED: audio table indices and the vehicle-sound teardown reached recycled arena memory](audio.md#fixed-audio-table-indices-and-the-vehicle-sound-teardown-reached-recycled-arena-memory)
- [MEASURED: absolute output level vs the real ROM — port/console is +0.016 dB](audio.md#measured-absolute-output-level-vs-the-real-rom--portconsole-is-0016-db)
- [FIXED: RAW16 bass was decoded as host-endian noise — wave "raw16"](audio.md#fixed-raw16-bass-was-decoded-as-host-endian-noise--wave-raw16)
- [M5 audio — DONE (music + SFX via the aspMain software mixer)](audio.md#m5-audio--done-music--sfx-via-the-aspmain-software-mixer)

**64-bit (LP64) and endianness — the dominant bug class**

- [FIXED: macOS packaged app was reported as “damaged”](portability.md#fixed-macos-packaged-app-was-reported-as-damaged)
- [FIXED: native representation boundaries — object tails, level records, and render proxies (wave "nativelayout")](allocator.md#fixed-native-representation-boundaries-object-tails-level-records-and-render-proxies--wave-nativelayout)
- [SWEPT: decomp arrays split into two C objects and indexed across the boundary (wave "splitsweep")](portability.md#swept-decomp-arrays-split-into-two-c-objects-and-indexed-across-the-boundary-wave-splitsweep)
- [LP64 pointer-truncation crash class — SYSTEMATIC SWEEP (robust: wave)](portability.md#lp64-pointer-truncation-crash-class--systematic-sweep-robust-wave)
- [\[RESOLVED in M3\] LP64 asset-struct layout — the blocker below is fixed](#resolved-in-m3-lp64-asset-struct-layout--the-blocker-below-is-fixed)
- [M3 BLOCKER — LP64 asset-struct layout (found in M2, the "first real frame" gate)](portability.md#m3-blocker--lp64-asset-struct-layout-found-in-m2-the-first-real-frame-gate)
- [From asset-swap workstream (M2-swap, commit b1600b2)](portability.md#from-asset-swap-workstream-m2-swap-commit-b1600b2)
- [Asset-swap gaps found while iterating M2](portability.md#asset-swap-gaps-found-while-iterating-m2)

**Browser (wasm) build**

- [FIXED: browser "memory access out of bounds" in the wave renderer — wave "wavetable"](web.md#fixed-browser-memory-access-out-of-bounds-in-the-wave-renderer--wave-wavetable)
- [FIXED: browser audio event-queue drops — measured in the real runtime](audio.md#fixed-browser-audio-event-queue-drops--measured-in-the-real-runtime)
- [M8 web (wasm) build — DONE (boots, runs, RENDERS correctly in-browser)](web.md#m8-web-wasm-build--done-boots-runs-renders-correctly-in-browser)

**Test harness and determinism**

- [FIXED: headless renders were NOT reproducible (wave "determinism")](renderer.md#fixed-headless-renders-were-not-reproducible-wave-determinism)
- [NOT A DEFECT, but zero coverage until now: character-select dancers reported static — wave "charselectmotion"](misc.md#not-a-defect-but-zero-coverage-until-now-character-select-dancers-reported-static--wave-charselectmotion)

**Deferred — none outstanding**

- [Integration pending](misc.md#integration-pending) — closed; kept for the
  hook-by-hook record of what had to be wired and where.
