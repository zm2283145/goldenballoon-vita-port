# Adventure Party admission — FOUR local players from the title.
#
# Char-select join/confirm timing mirrors race_4p_split.txt. Retail routes four
# Adventure players to TRACK_SELECT(15). With Enhancements.AdventureParty on, the
# AP-06 admission adapter routes them to GAME_SELECT(19) -> Adventure ->
# FILE_SELECT(6) and forms a 4-seat session.
#
# Save fixture: see tests/check_adventure_party_admission.py (started Adventure
# One save in slot 0, so the host's FILE_SELECT confirm resumes it).

# --- Title: reveal Start/Options, then Character Select ---
1250 START 4
1330 START 4

# --- PLAYER SELECT: players two through four join on their own controllers ---
1430 A 4 P2
1470 A 4 P3
1510 A 4 P4

# --- Each player confirms their own character ---
1600 A 4 P1
1660 A 4 P2
1720 A 4 P3
1780 A 4 P4

# --- "OK?" accept -> GAME_SELECT (on) / TRACK_SELECT (off) ---
1880 A 4 P1

# --- GAME_SELECT: Adventure One is option 0 -> FILE_SELECT ---
2100 A 4 P1

# --- FILE_SELECT: host (player one) resumes the started slot 0 ---
2350 A 4 P1
